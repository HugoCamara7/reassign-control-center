# Secretos: nunca subir credenciales de BigQuery
.streamlit/secrets.toml
*.json
!package.json

# Datos operativos
data/*.xls
data/*.xlsx
data/*.csv
outputs/
*.zip

# NOTA: config/prioridad_tiendas.xlsx SI se versiona. No contiene datos
# sensibles (codigos, nombres y orden de tiendas) y la app desplegada lo
# necesita: si no viaja en el repo, el servidor arranca sin prioridad.

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/

# Sistema
.DS_Store
Thumbs.db
~$*
